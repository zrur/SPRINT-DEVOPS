# =========================
# AZURE SQL DATABASE
# =========================
spring.datasource.url=jdbc:sqlserver://mottooth-sql-1449.database.windows.net:1433;database=mottoothdb;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=adminmottooth
spring.datasource.password=Prs417272@
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# HikariCP (connection pool)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# =========================
# Flyway
# =========================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true

# =========================
# JWT
# =========================
app.jwt.secret=5K8n1t3yZQfXjLwV9sA2mGhU7cR4oTbE6dP0vNjqYlBzCrFx
app.jwt.expiration=3600000

# =========================
# Server & Misc
# =========================
server.port=8080
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

spring.cache.type=simple

# Error Debug
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.thymeleaf=INFO

# Case sensitivity fix para SQL Server

# Schema explícito
spring.jpa.properties.hibernate.default_schema=dbo
spring.jpa.properties.hibernate.dialect.storage_engine=innodb

spring.jpa.properties.hibernate.default_schema=dbo
