# =========================
# ORACLE
# =========================
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
spring.datasource.username=RM556099
spring.datasource.password=fiap25
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.properties.hibernate.default_schema=RM556099
spring.jpa.open-in-view=false

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-test-query=SELECT 1 FROM DUAL

# =========================
# Flyway (ROTA B: come�ar do zero em schema "n�o vazio")
# =========================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.default-schema=RM556099
spring.flyway.schemas=RM556099
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true

# >>> chaves que resolvem o seu erro <<<
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
# (opcional, mas ajuda quando o schema ainda n�o existe fisicamente)
spring.flyway.create-schemas=true

# =========================
# Misc
# =========================
spring.sql.init.mode=never

server.port=8080
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

spring.cache.type=simple

server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
logging.level.org.springframework.web=DEBUG
logging.level.org.thymeleaf=DEBUG
